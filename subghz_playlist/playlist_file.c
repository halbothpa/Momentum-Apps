#include "playlist_file.h"

// Stub implementation for reduced repository
// This function is only needed for the Momentum firmware build
int playlist_count_playlist_items(Storage* storage, const char* file_path) {
    UNUSED(storage);
    UNUSED(file_path);
    return 0;
}
